<UserControl x:Class="PasswordManager.App.Views.TotpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:PasswordManager.App">

    <Grid Margin="28,22,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <StackPanel>
                <TextBlock Text="{Binding [Totp_Title], Source={x:Static loc:Localization.Instance}}" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="{Binding [Totp_Sub], Source={x:Static loc:Localization.Instance}}"
                           FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Content="{Binding [Totp_Add], Source={x:Static loc:Localization.Instance}}" Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Left" VerticalAlignment="Center" Padding="16,9" Click="AddButton_Click"/>
        </Grid>

        <ScrollViewer Grid.Row="2" Margin="0,18,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="AccountsPanel">
                <TextBlock x:Name="EmptyText" Text="{Binding [Totp_Empty], Source={x:Static loc:Localization.Instance}}"
                           Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center"
                           VerticalAlignment="Center" FontSize="14" Margin="0,60,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
